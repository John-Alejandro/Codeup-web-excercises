console.log("Break_and_Continue.js is in the \"House\".")

var x = 0;

var prompt("Pick a number between 1 and 50?");
while (false) {
    x++;
    if ( x % 2 === 0) {
        alert(" Pick another number.")
        else ( x % 3 ===)
    }
}